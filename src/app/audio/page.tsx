import { AppShell } from "@/components/app-shell";import { AudioStudio } from "@/components/audio-studio";import { FeatureUnavailable } from "@/components/feature-unavailable";import { isFeatureEnabled } from "@/lib/feature-flags";export const dynamic="force-dynamic";export default async function Page(){const enabled=await isFeatureEnabled("audio_studio");return <AppShell>{enabled?<AudioStudio/>:<FeatureUnavailable title="Audio Studio"/>}</AppShell>}

