import { AppShell } from "@/components/app-shell";import { MediaStudio } from "@/components/media-studio";import { TtsClient } from "@/components/tts-client";import { FeatureUnavailable } from "@/components/feature-unavailable";import { isFeatureEnabled } from "@/lib/feature-flags";export const dynamic="force-dynamic";export default async function Page(){const enabled=await isFeatureEnabled("audio_studio");return <AppShell>{enabled?<div style={{display:"grid",gap:24}}><div><div className="kicker">Audio studio</div><h1 className="page-title">Voice, sound & music</h1><p className="muted">Generate speech, sound effects and music from one PKR wallet.</p></div><TtsClient/><MediaStudio modality="audio" title="Sound & Music" subtitle="Choose a sound-effects or music model. Audio jobs that run asynchronously can be left processing in the background."/></div>:<FeatureUnavailable title="Audio Studio"/>}</AppShell>}
