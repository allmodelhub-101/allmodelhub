import { AppShell } from "@/components/app-shell";import { MediaStudio } from "@/components/media-studio";export const dynamic="force-dynamic";export default function Page(){return <AppShell><MediaStudio modality="video" title="Video Studio" subtitle="Create AI video with explicit cost confirmation, wallet reservations and asynchronous jobs designed for expensive generation."/></AppShell>}
