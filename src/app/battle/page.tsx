import { AppShell } from "@/components/app-shell";import { ModelBattle } from "@/components/model-battle";import { FeatureUnavailable } from "@/components/feature-unavailable";import { isFeatureEnabled } from "@/lib/feature-flags";export const dynamic="force-dynamic";export default async function Page(){const enabled=await isFeatureEnabled("model_battle");return <AppShell>{enabled?<ModelBattle/>:<FeatureUnavailable title="Model Battle"/>}</AppShell>}
