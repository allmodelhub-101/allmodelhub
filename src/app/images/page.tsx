import { AppShell } from "@/components/app-shell";import { MediaStudio } from "@/components/media-studio";export const dynamic="force-dynamic";export default function Page(){return <AppShell><MediaStudio modality="image" title="Image Studio" subtitle="Generate premium visuals with curated budget, balanced and premium image models. Exact model costs stay visible in Credits."/></AppShell>}
