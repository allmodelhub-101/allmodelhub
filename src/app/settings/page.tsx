import { SlidersHorizontal } from "@phosphor-icons/react/dist/ssr";import { AppShell } from "@/components/app-shell";import { SettingsClient } from "@/components/simple-forms";import { WorkspacePageHeader } from "@/components/workspace-page-header";import { requireUser } from "@/lib/auth";import { createAdminClient } from "@/lib/supabase/admin";export const dynamic="force-dynamic";export default async function Page(){const u=await requireUser();const a=createAdminClient();const {data:p}=await a.from('profiles').select('*').eq('id',u.id).single();return <AppShell><div className="workspace-page"><WorkspacePageHeader eyebrow="Preferences & safety" title="Settings" description="Control language, appearance, privacy, bandwidth, and personal spending protections." icon={<SlidersHorizontal weight="fill" aria-hidden="true"/>}/><SettingsClient initial={p||{}}/></div></AppShell>}


