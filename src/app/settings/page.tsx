import { AppShell } from "@/components/app-shell";import { SettingsClient } from "@/components/simple-forms";import { requireUser } from "@/lib/auth";import { createAdminClient } from "@/lib/supabase/admin";export const dynamic="force-dynamic";export default async function Page(){const u=await requireUser();const a=createAdminClient();const {data:p}=await a.from('profiles').select('*').eq('id',u.id).single();return <AppShell><div style={{marginBottom:20}}><div className="kicker">Preferences & safety</div><h1 className="page-title">Settings</h1></div><SettingsClient initial={p||{}}/></AppShell>}
