"use client";
import {FormEvent,useState} from "react";
import {ArrowRight,CheckCircle,Eye,EyeSlash,LockKey} from "@phosphor-icons/react";
import {createClient} from "@/lib/supabase/client";
export function UpdatePasswordForm(){const[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[show,setShow]=useState(false),[message,setMessage]=useState(""),[done,setDone]=useState(false),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(password!==confirm)return setMessage("Passwords do not match.");setBusy(true);const{error}=await createClient().auth.updateUser({password});setBusy(false);setMessage(error?error.message:"Password updated successfully. You can now continue to your workspace.");setDone(!error)}return <section className="auth-form-panel auth-update"><header className="auth-form-head"><span><LockKey weight="duotone"/></span><div><small>Secure recovery</small><h1>Choose a new password.</h1><p>Use at least eight characters and keep it unique.</p></div></header><form className="auth-form" onSubmit={submit}><label><span>New password</span><div className="auth-password"><input type={show?"text":"password"} minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/><button type="button" onClick={()=>setShow(!show)} aria-label={show?"Hide password":"Show password"}>{show?<EyeSlash/>:<Eye/>}</button></div></label><label><span>Confirm password</span><input type={show?"text":"password"} minLength={8} value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label><button className="auth-submit" disabled={busy||done}>{busy?"Updating…":done?"Password updated":"Update password"}<ArrowRight/></button></form>{message&&<div className={`auth-message ${done?"success":"error"}`}><CheckCircle/><span>{message}</span></div>}{done&&<a className="auth-google" href="/chat">Continue to workspace <ArrowRight/></a>}</section>}

